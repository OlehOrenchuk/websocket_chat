import styled from "styled-components";

const StyledNavbar = styled.nav`
  display: flex;
  justify-content: flex-end;
  width: 100%;
  padding: 20px;
  position: relative;
  z-index: 2;
`;

export { StyledNavbar };
